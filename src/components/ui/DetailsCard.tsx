import { Card, CardContent, CardHeader, CardTitle } from "./card";

export default function DetailsCard() {
  return (
    <Card className="h-full">
      <CardHeader>
        <CardTitle>Details</CardTitle>
      </CardHeader>
      <CardContent>det</CardContent>
    </Card>
  );
}
